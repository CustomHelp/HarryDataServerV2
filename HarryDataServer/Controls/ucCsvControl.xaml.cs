using System.Windows.Controls;

namespace HarryDataServer.Controls;

/// <summary>CSV export control (active path, rows, last write). DataContext is the MainViewModel.</summary>
public partial class ucCsvControl : UserControl
{
    public ucCsvControl() => InitializeComponent();
}
